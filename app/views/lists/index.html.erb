<div class="logo">
  <div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 mt-5">
      <h1>Lists</h1>
      <ul class="list-group">
        <% @lists.each do |list| %>
          <li class="list-group-item my-3"><span>
            <%= link_to list.name, list_path(list) %></span>
          </li>
        <% end %>
      </ul><span>
      <%= link_to "Add a new list", new_list_path, class: 'btn btn-primary mt-3' %>
   </span></div>
  </div>
</div>
